# ---- CPU core (search, metric, data prep, tests all run on these) ----
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
opencv-python>=4.8
scikit-image>=0.21
pillow>=10.0
pandas>=2.0
pyyaml>=6.0
matplotlib>=3.7
pytest>=7.4

# ---- GPU: generative training/inference (SDXL + LoRA + ControlNet) ----
# Install the torch build matching your CUDA from https://pytorch.org
torch>=2.1
diffusers>=0.27
transformers>=4.38
peft>=0.10
accelerate>=0.28

# ---- Search at scale ----
faiss-gpu>=1.7            # or faiss-cpu; sklearn fallback used if absent
open_clip_torch>=2.24

# ---- Serving optimisation ----
onnx>=1.15
# tensorrt        # install from NVIDIA (matches your CUDA/driver)
# tritonclient[http]

# ---- MLOps ----
mlflow>=2.11
dvc[s3]>=3.48            # or dvc[gs] / dvc[azure]
